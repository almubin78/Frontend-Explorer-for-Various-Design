import React from 'react';

const BatchAndChapterSelector = () => {
    return (
        <div>
            this is for batch and chapter selector
        </div>
    );
};

export default BatchAndChapterSelector;